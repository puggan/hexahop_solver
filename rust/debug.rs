pub fn run_debug(map_nr: usize, path: Option<String>) -> Result<(), String> {
    // In Rust, we return an Err instead of throwing an exception
    Err(format!("Debug function not implemented yet for map {} and path {:?}", map_nr, path))
}